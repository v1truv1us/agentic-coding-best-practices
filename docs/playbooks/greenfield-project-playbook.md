# Playbook: Greenfield Project with Agentic Coding

1. Draft spec using `docs/templates/spec-template.md`
2. Define architecture boundaries and contracts
3. Create phased plan (MVP → hardening)
4. Use subagents per bounded component
5. Enforce quality/security gates per milestone
6. Run integration review before merge
7. Capture lessons into reusable skills/templates
